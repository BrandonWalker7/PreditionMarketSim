# Frontend (Vite static build / local SPA)
# Injected at build time via vite.static.config.ts define.
NEXT_PUBLIC_SIGNALING_URL=http://localhost:3001

# Backend Nest signaling server
FRONTEND_ORIGIN=http://localhost:5173
PORT=3001

# Production (Render):
# NEXT_PUBLIC_SIGNALING_URL=https://preditionmarketsim.onrender.com
# FRONTEND_ORIGIN=<your static site URL, e.g. https://master-better-web.onrender.com>
